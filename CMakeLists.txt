# Минимальная требуемая версия CMake
cmake_minimum_required(VERSION 3.10)

# Название проекта
project(MyCoolApp)

# Добавляем наш исходный файл в исполняемый файл (executable)
add_executable(MyCoolApp main.cpp)
